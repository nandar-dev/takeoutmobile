import 'package:flutter/material.dart';

class Merchantshop extends StatelessWidget {
  const Merchantshop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("merchant shop"),
      ),
    );
  }
}